token = 'community token'
group_id = 'community id'

user = "database user"
password = "database password"
host = "database host"

admin_id = int('admin id')  # TODO: substitute with a list to add more admins

bot_commands = int('conversation id')

project_folder = "path to the project folder"
